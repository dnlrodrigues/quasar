const GET_GAME = (state, id) => {
  return state.listGame // state.listGame.filter(item => item.id === id)
}

export default {
  GET_GAME
}

/* export function getterGame (state, id) {
  return state.listGame.filter(item => item.id === id)
} */
